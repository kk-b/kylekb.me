/* One definition so the nav link and the about.txt CTA can't drift apart.
   The file lives in public/, which is copied to the site root at build. The
   path is relative rather than root-absolute so it resolves both at
   kylekb.me/ and under kk-b.github.io/kylekb.me/. */
export const RESUME_URL = './Kyle_KhuongBui_Resume_2026.pdf'
