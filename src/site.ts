/* One definition so the nav link and the about.txt CTA can't drift apart.
   The file itself lives in public/, which is copied to the site root at build,
   so this stays correct as long as the filename in public/ matches. */
export const RESUME_URL = '/Kyle_KhuongBui_Resume_2026.pdf'
